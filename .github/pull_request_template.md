## What and why

## Checklist
- [ ] `python3 -m unittest discover -s tests` passes
- [ ] Tests added or updated for behaviour changes
- [ ] No changes to `state.json` / `docs/upf.ics`, no secrets in the diff
- [ ] README updated if settings or user-facing behaviour changed
